<?xml version="1.0"?>
<model>
  <name>agriculture_visual</name>
  <version>1.0</version>
  <sdf version="1.9">model.sdf</sdf>
  <description>Visual-only Clearpath agriculture farm (no collision) so the Go1 walks on the flat ground_plane while the farm is scenery.</description>
</model>
